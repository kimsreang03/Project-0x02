import 'package:flutter/material.dart';
import 'package:paper_writer/pages/menu_bar.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return CreateMenuBar();
  }
}